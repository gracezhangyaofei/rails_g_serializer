require "spec_helper"

describe RailsGSerializer do
  it "has a version number" do
    expect(RailsGSerializer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
